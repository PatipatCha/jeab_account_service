generator db {
  provider = "go run github.com/prisma/prisma-client-go"
}

generator dbml {
  provider = "prisma-dbml-generator"
}

datasource db {
  provider = "postgresql"
  url      = "postgres://avnadmin:AVNS_6ji60WZsOHzY6m3z88V@pg-08082529-patipat-8829.aivencloud.com:19122/account_service?sslmode=require"
}

model Logging {
  id          Int           @id @default(autoincrement())
  created_by  String
  created_at  DateTime      @default(now())
  updated_at  DateTime?     @default(now())
  deleted_at  DateTime?     @default(now())

}

model users {
  id            Int          @id @default(autoincrement())
  username      String
  logging_id    Int
  userProfileId Int?
}

model personal {
  id         Int     @id @default(autoincrement())
  // user_id   User @relation(fields: [username], references: [username])
  username String
  logging_id Int
}
